# VieriAvarice 2.2.0.17

- Added an AutoDuty compatibility bridge that sends VieriAvarice's live rear and flank recommendations to Boss Mod movement during both VieriCodex handoffs and standalone AutoDuty runs.
- Clears the temporary positional request when AutoDuty stops without changing existing VieriAvarice, AutoDuty, or VieriCodex settings.

# VieriAvarice 2.2.0.16

- Added the shared Vieri logo to the Dalamud plugin listing without changing profiles or settings.

# 2.2.0.14

- Created the VieriAvarice upstream-compatible fork.
- Enabled native FFXIV UI masking by default for positional and range overlays.
- Added native-window overlap handling for compass letters and positional feedback icons.
- Added first-run migration from the existing Avarice configuration without modifying the original.
- Added `/vieriavarice` and `/va` commands.
# VieriAvarice 2.2.0.15

- Player, party-member, all-player, boss-center, and arena-center dots now explicitly hide behind native FFXIV windows.
- Keeps the existing native-UI masking behavior for all other positional overlays and preserves every profile setting.
